@model IEnumerable<PurchaseControlSystem.Models.Account>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Account_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Create_Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Created_by)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Closed_date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Closed_by)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cost_Center.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Expense.Short)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Account_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Create_Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Created_by)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Closed_date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Closed_by)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cost_Center.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Expense.Short)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Account_Code }) |
                @Html.ActionLink("Details", "Details", new { id=item.Account_Code }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Account_Code })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
