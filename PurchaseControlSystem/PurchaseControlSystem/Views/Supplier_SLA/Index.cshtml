@model IEnumerable<PurchaseControlSystem.Models.Supplier_SLA>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SLA)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Supplier.Short)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
               @Html.DisplayFor(modelItem => item.SLA)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Supplier.Short)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Supplier_SLAId }) |
                @Html.ActionLink("Details", "Details", new { id=item.Supplier_SLAId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Supplier_SLAId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
