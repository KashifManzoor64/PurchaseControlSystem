@model IEnumerable<PurchaseControlSystem.Models.VAT_Code>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.VATCodeDesc)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VATCodeDesc)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.VAT_Code1 }) |
                @Html.ActionLink("Details", "Details", new { id=item.VAT_Code1 }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.VAT_Code1 })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
